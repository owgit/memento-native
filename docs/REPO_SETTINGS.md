# GitHub Repository Settings Checklist

These settings are outside the codebase and must be configured in the repository UI.

## About section

Set these values in repository settings:

- Description:
  - `Local-first macOS timeline memory with OCR and semantic search (Swift + ScreenCaptureKit)`
- Website:
  - `https://owgit.github.io/memento-native/`
- Topics:
  - `macos`
  - `swift`
  - `ocr`
  - `semantic-search`
  - `privacy`
  - `screen-recording`
  - `screencapturekit`
  - `local-first`

## Social preview image

Upload a social preview image aligned with README messaging:

- Project: `Memento Native`
- Keywords: `local-first`, `OCR`, `semantic search`, `timeline memory`
- Recommended size: 1280x640

## GitHub Pages

Publish the download landing page from:

- Source: `Deploy from a branch`
- Branch: `main`
- Folder: `/docs`

The landing page entry point is `docs/index.html`, and the primary download CTA points to the latest GitHub Release.

## Discussions

Enable Discussions and create categories:

- `Q&A`
- `Ideas`
- `Troubleshooting`
- `Announcements`

Pin an announcement post for:

- current release highlights
- support routing (Discussions vs Issues)
- FAQ and release docs links

## Notifications and release visibility

- Ensure release publishing is part of maintainer checklist
- Keep release notes linked from README and CHANGELOG
- For major updates, post and pin a Discussion summary
